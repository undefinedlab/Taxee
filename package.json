{
  "scripts": {
    "deploy:contracts": "hardhat run deploy/deploy.js --network baseSepolia",
    "verify:contracts": "echo 'Run: npx hardhat verify --network baseSepolia <CONTRACT_ADDRESS> [ARGS]'",
    "test:contracts": "hardhat test",
    "compile:contracts": "hardhat compile"
  },
  "dependencies": {
    "@gsap/react": "^2.1.2",
    "gsap": "^3.15.0"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^6.1.2",
    "dotenv": "^17.4.2",
    "hardhat": "^2.28.6",
    "ts-node": "^10.9.2",
    "typescript": "^5.7.2"
  }
}
